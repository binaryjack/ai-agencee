{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ai-agencee.io/schemas/pack.schema.json",
  "title": "TechPack",
  "description": "Schema for a technology pack (.pack.md frontmatter)",
  "type": "object",
  "required": ["name", "version", "description"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "Kebab-case pack identifier matching the filename"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semver version string"
    },
    "description": {
      "type": "string",
      "minLength": 10,
      "description": "Short human-readable description of what the pack covers"
    },
    "frameworks": {
      "type": "string",
      "description": "Comma-separated list of meta-frameworks this pack targets (optional)"
    }
  }
}
